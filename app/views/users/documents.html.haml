.super-hed
  = link_to "#{@user.name}'s", people_path(@user.id, @user.name.parameterize)
%h1.section-hed
  #{atomic_unit}s

  %small
    = link_to 'new', new_document_path, class: 'btn btn-default btn-xs', style: 'margin: 0 0 8px 8px;'

= render partial: 'documents/tiles', locals: {collectibles: @collectibles}

= will_paginate @collectibles, renderer: BootstrapPagination::Rails