%h3{style: 'margin-bottom: 20px;'}
  Your documents
  %small
    = link_to 'new', new_document_path, class: 'btn btn-primary btn-xs', style: 'margin: 0 0 8px 8px;'

= render partial: 'documents/tiles', locals: {documents: @documents}

= will_paginate @documents, renderer: BootstrapPagination::Rails