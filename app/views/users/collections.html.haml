.super-hed
  = link_to "#{@user.name}'s", people_path(@user.id, @user.name.parameterize)
%h1.section-hed
  Collections
  
  %small
    = link_to 'new', new_collection_path, class: 'btn btn-default btn-xs', style: 'margin: 0 0 8px 8px;'

= render partial: 'collections/tiles', locals: {collections: @collections}

= will_paginate @collections, renderer: BootstrapPagination::Rails