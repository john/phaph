%h1 Labs

%table.table

  - @labs.each do |lab|
    %tr
      %td= link_to lab.name, lab
      %td= link_to 'Show', lab
      %td= link_to 'Edit', edit_lab_path(lab)
      %td= link_to 'Destroy', lab, :method => :delete, :data => { :confirm => 'Are you certain?' }

%br

= link_to 'New Lab', new_lab_path, :class => 'btn btn-primary'
